<Window x:Class="SyncMLViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SyncMLViewer"
        mc:Ignorable="d"
        Title="SyncML Viewer - oliverkieselbach.com" Height="800" Width="1124" Closed="Window_Closed" MinWidth="1124">
    <Window.Resources>
        <HierarchicalDataTemplate x:Key="NodeTemplate" ItemsSource="{Binding XPath=./*}">
            <TextBlock x:Name="nodetext"/>
            <HierarchicalDataTemplate.Triggers>
                <DataTrigger Binding="{Binding Path=NodeType}" Value="Element">
                    <Setter TargetName="nodetext" Property="Text" Value="{Binding Path=Name}" />
                </DataTrigger>
            </HierarchicalDataTemplate.Triggers>
        </HierarchicalDataTemplate>
    </Window.Resources>
    <Grid>
        <avalonEdit:TextEditor
            xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
            Name="textEditor"
            SyntaxHighlighting="XML"
            FontFamily="Consolas"
            FontSize="10pt" Margin="302,10,9.6,43.4"
            HorizontalScrollBarVisibility="Auto"
            ShowLineNumbers="True"
            IsReadOnly="True"/>
        <CheckBox Content="HTML Decode" HorizontalAlignment="Left" Margin="10,0,0,16.4" Checked="CheckBoxHtmlDecode_Checked" Height="15" VerticalAlignment="Bottom"/>
        <Button x:Name="buttonClear" Content="Clear All" Margin="0,0,252.6,9.4" Click="ButtonClear_Click" Height="29" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="97"/>
        <Button x:Name="buttonSaveAs" Content="Save As" Margin="0,0,140.6,9.4" Click="ButtonSaveAs_Click" Height="29" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="97"/>
        <Button x:Name="buttonSync" Content="Sync" Margin="0,0,16,9" Click="ButtonSync_Click" Height="29" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="97"/>
        <TreeView x:Name="treeViewProtocol" HorizontalAlignment="Left" Height="717" Margin="10,10,0,0" VerticalAlignment="Top" Width="287" ItemsSource="{Binding}" ItemTemplate="{StaticResource NodeTemplate}"/>
        <Button x:Name="buttonLoadTreeView" Content="Load" Margin="0,0,366.6,9.4" Click="ButtonLoadTreeView_Click" Height="29" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="97"/>
    </Grid>
</Window>
